# Rhino + Grasshopper plugin

A single Python file. Drop into Rhino's `Scripts` folder, then:

```
RunPythonScript "vex_bridge.py"
```

Or wire it to a toolbar button. There is intentionally nothing else.

See [`vex_bridge.py`](vex_bridge.py).
